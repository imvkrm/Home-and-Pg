<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vikram.myo">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/llogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Base"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Splash"
            android:label="MYO"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Base1"
            android:label="@string/title_activity_base1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Base2"
            android:label="@string/title_activity_base2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BaseN"
            android:label="@string/title_activity_base_n"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Map1"
            android:label="@string/title_activity_map1" />
        <activity
            android:name=".Base3"
            android:label="@string/title_activity_base3"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Base4"
            android:label="@string/title_activity_base4"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Basedes"
            android:label="@string/title_activity_basedes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Signup" />
        <activity
            android:name=".Chandigarh"
            android:label="@string/title_activity_chandigarh"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Description"
            android:label="@string/title_activity_description"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity1"
            android:label="@string/title_activity_main1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Delhi" />
        <activity android:name=".Gurgaon"></activity>
    </application>

</manifest>